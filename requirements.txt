# Python deps for the JAX alpha-policy training (nn/).
# Pinned to the versions validated locally.
# For CPU:   jax==0.10.2
# For CUDA:  jax[cuda12]==0.10.2
jax[cuda12]==0.10.2
equinox==0.13.8
optax==0.2.8
numpy==2.4.6
matplotlib==3.11.0