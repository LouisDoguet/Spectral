# Python deps for the JAX alpha-policy training (nn/).
# Pinned to the versions validated locally (Python 3.13, CPU jaxlib).
# For CUDA nodes replace the two jax lines with:  jax[cuda12]==0.10.2
jax[cuda12]==0.10.2
jaxlib==0.10.2
equinox==0.13.8
optax==0.2.8
numpy==2.4.6
matplotlib==3.11.0
